// Compatibility entry point: the two-section seam was replaced by one header.
// The row regression also checks that inherited HA section gaps have no effect.
await import('./check-home-header-row.mjs');
