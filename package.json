{
  "name": "home-dashboard",
  "version": "0.3.0-alpha.2",
  "private": true,
  "type": "module",
  "description": "HACS-installable community dashboard strategy for Home Assistant",
  "license": "MIT",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "pnpm typecheck && pnpm build && node scripts/verify-dist.mjs && node --check prototype/app.js && node --check prototype/fixtures.js && node --check prototype/editor-harness.js && node scripts/check-repo.mjs",
    "test": "pnpm check && node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "release:assets": "node scripts/create-release-assets.mjs",
    "serve": "node scripts/serve-prototype.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
